Officer headshots.

Drop the raw files here with any name — I'll resize, strip EXIF, rename to
match the officer slug, and wire up content/officers/*.json.

Final files should be square, ~800x800, under 300KB. A card with no photo
shows a plain block rather than a broken image.
